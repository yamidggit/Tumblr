
<%@pots.each do |post|%>
    <div class="post_wrapper">
        <h2 class= "title"><%= link_to post.title, post %></h2>
        <p class="body"><%= truncate(post.content, length:100)%></p>
        <p class= "date"><%= time_ago_in_words(post.created_at) %></p>
        
    </div>
<%end%>